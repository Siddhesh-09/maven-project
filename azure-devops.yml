trigger:
- main       #LIST       # Runs when changes are pushed to the 'main' branch
- master
- devops

pool:
  vmImage: 
    - 'ubuntu-latest'  #LIST      # Use Microsoft-hosted Ubuntu agent

steps:
- script: mvn validate
  displayName: 'Validate Maven Project'